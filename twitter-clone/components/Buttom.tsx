interface ButtomProps {
    label: string;
    secondary: boolean;
    fullWidth: boolean;
    large: boolean;
    onClick: () => void;
    disabled: boolean;
    outline: boolean;
}

const Buttom: React.FC<ButtomProps> = ({ label, secondary, fullWidth, large, onClick, disabled, outline}) => {
  return (
    
  )
}

export default Buttom